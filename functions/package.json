{
  "name": "heeroo-functions",
  "private": true,
  "description": "Fonctions serveur Heeroo (notifications, comptes, commission chauffeur)",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy:dev": "firebase deploy --only functions --project dev",
    "deploy:prod": "firebase deploy --only functions --project prod",
    "logs": "firebase functions:log",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "stripe": "^22.6.2"
  }
}
